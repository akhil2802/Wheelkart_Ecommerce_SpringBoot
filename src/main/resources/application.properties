
# MySQL:
spring.datasource.url=jdbc:mysql://localhost:3306/full-stack-ecommerce?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=ecommerceapp
spring.datasource.password=ecommerceapp
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG

# API Expose Strategy:
spring.data.rest.detection-strategy=ANNOTATED

# Cors:
spring.data.rest.base-path=/api
allowed.origins=https://localhost:4200

# Okta:
okta.oauth2.client-id=0oa9q8s78r15hmgcV5d7
okta.oauth2.client-secret=ySjo4YwnKmLLxl5fBW7e7Nifj7aZkk0oLWK1UPt5
okta.oauth2.issuer=https://dev-94016314.okta.com/oauth2/default

# application.properties
management.endpoints.web.exposure.include=*

#####
#
# HTTPS configuration
#
#####

# Server web port
server.port=8443

# Enable HTTPS support (only accept HTTPS requests)
server.ssl.enabled=true

# Alias that identifies the key in the key store
server.ssl.key-alias=wheelkart

# Keystore location
server.ssl.key-store=classpath:wheelkart-keystore.p12

# Keystore password
server.ssl.key-store-password=secret

# Keystore format
server.ssl.key-store-type=PKCS12

